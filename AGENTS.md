# EBASE 开发强约束

本文件对本仓库中的所有开发者、AI Agent 和自动化工具生效。开始任何任务前，必须先阅读本文件及 `docs/DEVELOPMENT-STANDARDS.md`。不得以“快速实现”“临时方案”或“原型阶段”为由绕过标记为 **MUST** 的规则。

## 1. 固定技术栈

- 前端 **MUST** 使用 Vue 3、TypeScript、Vite、Vue Router；组件使用 Composition API 与 `<script setup lang="ts">`。
- 后端 **MUST** 使用 ThinkPHP 8，提供版本化 REST API。
- 核心业务数据 **MUST** 存 MySQL；Redis 仅用于缓存、锁、验证码/会话、队列和热点数据，不能替代业务事实库。
- 登录 **MUST** 使用 JWT/Token；生产环境不得使用当前原型中的 `localStorage` 会话作为认证依据。
- 图标 **MUST** 优先使用 `lucide-vue-next`；禁止混入 emoji、字符图标或来源不明的 SVG。
- 未经架构评审，不得替换框架、引入第二套 UI 组件库、状态管理库或 CSS 框架。

## 2. 不得破坏的产品边界

- “用户”表示消费者/会员；“成员”表示使用后台的内部员工。两者的数据模型、路由、权限和接口 **MUST** 分离。
- 当前仓库是统一运营后台，不是消费者商城前台。独立站管理能力在此扩展，消费者 Storefront 必须独立建应用并共享同一 API 与业务事实库。
- 商品主数据与渠道商品配置 **MUST** 分离；独立站专属标题、描述、Slug、价格、媒体、SEO 和发布状态不得覆盖国内平台字段。
- 认证页面 **MUST** 使用独立 `auth` 布局；后台页面 **MUST** 使用 `AppShell`。
- 每个新增业务功能至少包含：列表、查询/筛选、空状态、加载状态、错误状态，以及适用时的新增、查看、编辑、删除和删除确认。
- 所有写操作 **MUST** 有明确成功/失败反馈；危险操作 **MUST** 二次确认。
- 权限不得只做前端按钮隐藏。前端负责可见性，后端中间件负责最终授权。

## 3. API 与数据强约束

- API 前缀 **MUST** 为 `/api/v1`；响应结构、分页、错误码遵循开发规范。
- 前端 **MUST** 通过统一 API Client 访问后端；禁止在 Vue 组件中散落 `fetch`/`axios` 调用。
- 请求参数 **MUST** 在后端使用独立 Validate 类校验；控制器不得手写重复校验。
- 金额 **MUST** 使用最小货币单位整数或 DECIMAL，禁止浮点数计算。
- 库存扣减、回补、支付状态迁移、退款 **MUST** 幂等并处于事务或可靠事件流程中。
- MySQL 表 **MUST** 包含必要的创建/更新时间；软删除是否启用须按领域统一，不得单接口自创规则。
- Redis Key **MUST** 使用 `ebase:{env}:{domain}:{purpose}:{id}` 命名并设置合理 TTL；库存锁必须设置过期时间和唯一持有者标识。

## 4. 安全与权限强约束

- 密码 **MUST** 使用安全哈希；日志、响应、异常中禁止输出密码、Token、支付密钥、验证码和完整敏感信息。
- Access Token 应短期有效；Refresh Token 必须可撤销、可轮换，并记录设备会话。
- 所有管理 API 默认拒绝访问，只有显式授权的角色/权限码可通过。
- 支付回调 **MUST** 验签、校验金额与商户订单、保证幂等，并保留原始回调审计记录。
- 上传 **MUST** 校验 MIME、扩展名、大小和访问权限；禁止信任客户端文件名。

## 5. UI 与代码质量强约束

- 保持现有清透、紧凑的后台视觉系统；优先复用 CSS 变量和既有组件，不得另起一套圆角、阴影、字体或间距体系。
- 表单字体必须继承全局字体；不得在局部使用浏览器默认字体。
- 页面内容必须在 320px、760px、1100px、1440px 关键宽度下无横向裁剪；数据表允许表格容器内部滚动，页面主体不得整体漂移。
- 禁止直接修改 `dist/`、生成的 `.js`、`*.tsbuildinfo`；源码以 `.vue`、`.ts`、`.css` 为准。
- 改动完成后 **MUST** 运行 `npm run build`。后端加入后还必须运行对应测试和静态检查。
- 不得删除或覆盖与当前任务无关的用户改动。

## 6. 变更流程

1. 阅读 `docs/PROJECT-INVENTORY.md`，确认已有页面与功能，避免重复建设。
2. 阅读 `docs/DEVELOPMENT-STANDARDS.md`，确认接口、目录、权限和数据边界。
3. 先复用已有页面模式；新抽象必须至少有两个真实使用点。
4. 更新实现、类型、错误/空/加载状态和测试。
5. 运行构建与必要测试。
6. 若新增、删除或改变页面/API/领域规则，必须同步更新 `docs/PROJECT-INVENTORY.md` 和开发规范。

Git 提交信息 **MUST** 使用简洁中文，格式建议为 `类型：变更摘要`，例如 `修复：完善导航活动状态`。除非用户明确要求，不得使用英文提交信息。

若用户明确要求与本文件冲突，必须指出冲突及风险并获得明确确认；不得静默偏离。
